/** @type {import("tailwindcss").Config} */
const config = {
  darkMode: "class",
  theme: {
    extend: {
      fontFamily: {
        sans: ["Inter", "Noto Sans", "sans-serif"],
      },
    },
  },
  plugins: [],
};

export default config;
